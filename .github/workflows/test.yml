name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: adoptopenjdk/openjdk12:x86_64-alpine-jre-12.0.2_10

    steps:
    - uses: actions/checkout@v1
    - name: Test
      run: ./gradlew test
